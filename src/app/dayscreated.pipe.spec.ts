import { DayscreatedPipe } from './dayscreated.pipe';

describe('DayscreatedPipe', () => {
  it('create an instance', () => {
    const pipe = new DayscreatedPipe();
    expect(pipe).toBeTruthy();
  });
});
